# ---- 监听 ----
# AMQP 0-9-1 / AMQP 1.0
listeners.tcp.default = 5672

# ---- Management 插件 ----
management.tcp.port = 15672
management.tcp.ip   = 0.0.0.0

# ---- 开发环境资源水位（沿用默认即可，如需收紧再调整） ----
# vm_memory_high_watermark.relative = 0.4
# disk_free_limit.absolute = 2GB
