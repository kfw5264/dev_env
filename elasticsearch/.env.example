# ============================================================
# dev-elasticsearch 环境变量示例文件
# 用法：复制为 .env 后按需修改
#   cp .env.example .env
# ============================================================
# 说明：当前 elasticsearch/docker-compose.yml 未使用任何 ${VAR} 插值，
#       即本示例中的变量目前【不会生效】，保持空文件复制即可。
# 若希望用 .env 管理以下配置，请先在 docker-compose.yml 中把硬编码值
# 改为 ${VAR:-默认值} 插值（下方注释给出与现配置一致的默认值，供参考）：
#
#   # 对外端口
#   ES_HTTP_PORT=9200
#   # 集群名
#   ES_CLUSTER_NAME=dev-es
#   # JVM 堆内存参数
#   ES_JAVA_OPTS=-Xms1g -Xmx1g
#   # 时区
#   TZ=Asia/Shanghai
