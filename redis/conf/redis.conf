# 监听所有网卡（容器内），端口映射到主机
bind 0.0.0.0
port 6379

# 密码通过 docker-compose.yml 中的 --requirepass 注入（默认 redis123）

# ---- 持久化 ----
dir /data
appendonly yes
appendfsync everysec
save 900 1
save 300 10
save 60 10000

# ---- 内存 ----
maxmemory 512mb
maxmemory-policy noeviction

# ---- 日志 ----
loglevel notice
